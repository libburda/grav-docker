kind: pipeline
type: docker
name: build

trigger:
  branch:
    - master
  repo:
    - getgrav/grav
  event:
    - push

steps:
  - name: docker
    image: plugins/docker
    settings:
      username:
        from_secret: docker_username
      password:
        from_secret: docker_password
      repo: liborburda/grav
      tags: latest
